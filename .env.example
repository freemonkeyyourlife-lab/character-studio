# Hugging Face token with inference permissions.
# Keep this secret on the server and never expose it as NEXT_PUBLIC_*.
HF_TOKEN=
# Optional model override. Default: black-forest-labs/FLUX.1-schnell
HF_IMAGE_MODEL=black-forest-labs/FLUX.1-schnell
